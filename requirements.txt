# Core RAG stack
chromadb>=0.4.22
sentence-transformers>=2.6.0
langchain>=0.1.0
langchain-community>=0.0.20

# PDF ingestion
pypdf>=3.17.0

# LLM backends (install whichever you're using)
anthropic>=0.20.0        # for Claude — recommended
# openai>=1.10.0         # uncomment for GPT-4o-mini

# Web UI
streamlit>=1.32.0

# Notebook / evaluation
jupyter>=1.0.0
matplotlib>=3.8.0
numpy>=1.26.0

# Optional: local model fallback (large download, skip if you have an API key)
# transformers>=4.38.0
# torch>=2.2.0
