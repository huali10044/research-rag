# For a reproducible environment (exact versions this app was built/tested
# against), install requirements-lock.txt instead:
#   pip install -r requirements-lock.txt
# Note: the lock file was generated via `pip freeze` on macOS and is meant for
# local dev reproducibility. It is not platform-tagged, so a couple of pins
# (e.g. torch, chromadb's compiled deps) may not resolve identically on
# Linux/Windows. Streamlit Cloud deploys from this file (requirements.txt),
# not the lock file.

# Core RAG stack
chromadb>=0.4.22
sentence-transformers>=2.6.0
pypdf>=3.17.0

# LLM backends — install whichever you're using
# Free tier:
google-genai>=1.0.0        # Gemini (free tier) — aistudio.google.com
openai>=1.10.0             # also used by groq, mistral, openrouter, ollama (OpenAI-compatible)
cohere>=5.0.0              # Cohere Command-R (free trial) — dashboard.cohere.com
# Paid:
anthropic>=0.20.0          # Claude — console.anthropic.com

# Web UI
streamlit>=1.32.0

# Notebook / evaluation
jupyter>=1.0.0
matplotlib>=3.8.0
numpy>=1.26.0
