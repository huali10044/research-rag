[llm]
# Default backend to use. Options: gemini, anthropic, groq, mistral, openrouter, cohere, ollama, local, auto
backend = "gemini"

# Default model override. Leave empty ("") to use each backend's default model.
model = "gemini-3.5-flash-lite"
